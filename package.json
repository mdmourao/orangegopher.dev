{
  "name": "orangegopher.dev",
  "version": "1.0.0",
  "private": true,
  "description": "Open source Go adapters and proxy Workers for Cloudflare tools.",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name orangegopher-dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
